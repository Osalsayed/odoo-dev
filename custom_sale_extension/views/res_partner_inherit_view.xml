<odoo>
                     <!--    form    -->
    <record id="view_partner_form_inherit_custom" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.customer.code</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="after">
                <field name="customer_code" readonly="1" groups="sales_team.group_sale_salesman_all_leads"/>
                <field name="creation_order" readonly="1" groups="sales_team.group_sale_salesman_all_leads"/>
            </xpath>
        </field>
    </record>

                      <!--    tree    -->
     <record id="view_partner_tree_inherit_customer_code" model="ir.ui.view">
        <field name="name">res.partner.tree.inherit.customer_code</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree/field[@name='translated_display_name']" position="after">
                <field name="customer_code" string="Customer Code" groups="sales_team.group_sale_salesman_all_leads"/>
                <field name="creation_order" string="Creation Order" groups="sales_team.group_sale_salesman_all_leads"/>
            </xpath>
        </field>
    </record>
                     <!--    search    -->
    <record id="view_partner_search_inherit_customer_code" model="ir.ui.view">
        <field name="name">res.partner.search.inherit.customer_code</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']" position="after">
                <field name="customer_code" groups="sales_team.group_sale_salesman_all_leads"/>
            </xpath>

            <xpath expr="//field[@name='name']" position="attributes">
                <attribute name="filter_domain">
                    ['|', '|', '|', '|', '|', ('display_name', 'ilike', self),
                     ('ref', 'ilike', self), ('email', 'ilike', self),
                     ('vat', 'ilike', self), ('company_registry', 'ilike', self),
                     ('customer_code', 'ilike', self)]
                </attribute>
            </xpath>

        </field>
    </record>

</odoo>
